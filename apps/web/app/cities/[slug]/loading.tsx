import { SiteChromeSkeleton } from '@/components/SiteChromeSkeleton';

/** Soft-nav / streaming: same fixed night-hero shell as CityPageView (CLS-safe). */
export default function CitySlugLoading() {
  return <SiteChromeSkeleton variant="city" />;
}
