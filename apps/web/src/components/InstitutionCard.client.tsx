'use client';

/**
 * @deprecated Use LocationCard - one catalog card for all Venue kinds.
 * Kept as a thin alias so older imports keep compiling.
 */
export { LocationCard as InstitutionCard } from '@/components/LocationCard.client';
